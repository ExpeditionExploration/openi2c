{
    "targets": [
        {
            # 1) Build the Linux driver using the Makefile in ./src/Makefile
            # 2) Add node-gyp to package.json
            # 3) Describe the API in this .gyp file.
            # 4) Describe generating bindings, and refer to the driver header 
            #    files in the README for the driver api.
            # 5) Write higher level OO api for the driver.
            # 6) Integrate with package build process.
        }
    ]
}
